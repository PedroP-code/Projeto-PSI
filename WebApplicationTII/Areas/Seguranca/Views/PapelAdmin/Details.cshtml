@using WebApplicationTII.Infraestrutura
@using WebApplicationTII.Areas.Seguranca.Models
@model PapelDetailsModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Visualizando os dados de uma ROLE";
}

<div class="panel panel-primary">

    <div class="panel-heading">
        Papel
    </div>
    <div class="panel-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Role</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Model.Papel.Id</td>
                    <td>@Model.Papel.Name</td>
                </tr>
            </tbody>
        </table>
    </div>
    @*<div class="panel-footer panel-info">
            @Html.ActionLink("Alterar", "Edit", new { id = Model.Papel.Id }, new { @class = "btn btn-info" });
            @Html.ActionLink("Retornar para a listagem", "Index", null, new { @class = "btn btn-info" })
        </div>*@
</div>
@Html.Partial("~/Areas/Seguranca/Views/PapelAdmin/_PartialDetailsContentPanel.cshtml", Model)
@section ScriptPage {
    <script type="text/javascript">
        $(document).ready(function () {
            $('li').removeClass("active");
            $('#liPapel').addClass("active");
        });
    </script>
}